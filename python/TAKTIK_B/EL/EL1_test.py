from EL1 import solve

def test_document1():
    assert(solve(6, ["3x", "-2y", "-5x", "0y"]) == [
"┘┘┐┐┐┐",
"┌┐┌┌┌┌",
"└┌└└└└",
"┌┐┌┌┌┌",
"┌┐┌┌┌┌",
"┌┐┌┌┌┌"
])
    
def test_document2():
    assert(solve(8, ["0x", "-0y", "8x", "3y"]) == [
"┌┌└┌┌┌┌┌",
"┌┌└┌┌┌┌┌",
"┌┌└┌┌┌┌┌",
"┌┌└┌┌┌┌┌",
"┌┌└┌┌┌┌┌",
"┌┌└┌┌┌┌┌",
"┌┌└┌┌┌┌┌",
"└└┘└└└└└"
])
    
def test_document3():
    assert(solve(4, ["-2y", "3x", "-2y"]) == [
"└┐└└",
"┌┐┌┌",
"┌┐┌┌",
"┌┐┌┌"
])